# Checkpoints

Training checkpoints, LoRA adapters, optimizer state, and merged model weights are written here by default and are excluded from Git.

For staged training, point `CKPT_PATH` or `STAGE1_LORA_CHECKPOINT` to the Stage 1 adapter directory before starting the next stage.
